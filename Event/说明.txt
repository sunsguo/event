

还有些问题：
使用ajax删除活动时，抛出异常，还未找到问题


学到的东西：
php 的面向对象编程
php 的魔术方法 __autoload($classname)
php 中常用时间api time() date() strtotime()
php 中true, TRUE , == , === , isset() , empty() 等的区别


用jQuery 实现了ajax web 程序 $.ajax({......});
使用ajax时，提供的参数必须经过编码
学会了时用各种 jQuery 的淡入淡出动画

学会了css 中用 aphta 滤镜实现背景半透明鲜果
使用z-index 属性实现覆盖层效果

模态窗口的使用


拓展：
可以使用jQuery ui 实现模态窗口的拖拽